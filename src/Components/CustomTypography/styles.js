import { styled } from '@mui/material/styles'
import { Typography } from '@mui/material'

export const StyledH3 = styled(Typography)(() => ({
    color: 'white',
    opacity: '0.8',
    fontWeight: '600',
    marginBottom: '30px',
}))

export const StyledH5 = styled(Typography)(() => ({
    color: 'white',
    opacity: '0.8',
}))

export const StyledH6 = styled(Typography)(() => ({
    color: 'white',
    opacity: '0.8',
    fontSize: '1.125rem',
}))

export const StyledSubtitle = styled(Typography)(() => ({
    color: 'white',
    opacity: '0.8',
}))
